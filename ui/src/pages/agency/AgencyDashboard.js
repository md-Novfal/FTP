import React from 'react';
import { Typography, Box } from '@mui/material';

function AgencyDashboard() {
  return (
    <Box>
      <Typography variant="h5" fontWeight={700} mb={3}>Agency Dashboard</Typography>
      {/* TODO: Students summary, recent applications, commission status */}
    </Box>
  );
}

export default AgencyDashboard;
