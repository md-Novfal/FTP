{
  "name": "fto-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "FTO.EDU Admission Portal — Monorepo",
  "workspaces": ["ui", "server"],
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix server\" \"npm start --prefix ui\"",
    "install:all": "npm install --prefix server && npm install --prefix ui",
    "build:ui": "npm run build --prefix ui",
    "test": "npm test --prefix server && npm test --prefix ui",
    "lint": "npm run lint --prefix server && npm run lint --prefix ui"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
